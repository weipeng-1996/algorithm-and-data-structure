 while i < l:
        j = i
        while j != -1:
            arr_count[i] += 1
            if arr[i] > stack[j]:
                arr_count[i] += 1
        if not stack or arr[i] > stack[-1]:
            stack
        else:

        stack.push(arr[i])
        for j in range(i, l)
          for i in range(1, l-1):
        arr_count[i] += 1



######
   while i < l and :
        right = i + 1
        rmax = arr[i]
        while right < l:
        left = i - 2
        lmax = arr[i-1]
        while left > -1:
            if stack[j] > lmax:
                arr_count[i] += 1
                lmax = stack[j]
            left -= 1
        
